Host load average 11-26 on 10 cores from unrelated sessions (IDEs, other builds). c50 was tight; c200 rpc and c500 raw collapsed with both processes mostly idle (client cpu 0.26-0.38). CAUSE NOT CONFIRMED: host load is a plausible explanation and the same configs were tight in the *-rerun directories at load ~7.5, but an idle-both-sides collapse is also consistent with a wakeup/wait problem. Treat these as anomalous runs of unconfirmed cause. Per-connection progress was not recorded by the harness at the time (added afterwards), so stuck-connection vs global slowdown cannot be told apart for these runs.
